[Metrics]
active: ${metric_name}
${metric_name}: {
         "type": "${metric_type}",
         "path": "%(externalDir)s/metric.tsv",
         "fields": ["${field_name}"],
         "colors": ["#f11", "#1d1"],
         "percentile": true,
         "neutralColor": "#bbb",
         "maxValue": 1.0
         }